language: node_js
node_js:
  - '0.10'
  <% if (!props.karma) { %>- '0.11'<%}%>
before_script:
  <% if (props.karma) { %>- 'export DISPLAY=:99.0'
  - 'sh -e /etc/init.d/xvfb start'<%}%>
  - npm install -g grunt-cli
  <% if (props.karma) { %>- npm install -g bower
  - bower install<%}%>
<% if (!props.karma) { %>
matrix:
  allow_failures:
    - node_js: '0.11'<%}%>